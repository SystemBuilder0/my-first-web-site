# 2026-09-11 Claude→ChatGPT 컨텍스트 이전 — 판단 기록 (CLAUDE.md 9조)

세션 모델: Fable 5.1 (claude-fable-5-1). 원래 저장 위치 `G:\공유폴더\claude code study\fable5 근거 모음\`은 이 클라우드 세션에서 접근 불가라 이 폴더에 대신 둔다. PC에서 해당 폴더로 옮기고 `INDEX.md`에 마지막 줄을 추가할 것.

## [03:06] 턴 1

### 판단 1 — "이 환경에 과거 대화가 없다"에서 멈출 것인가
- 분기점: 컨테이너에 세션 기록이 현재 것 1개뿐임을 확인한 직후.
- 선택지: (a) "claude.ai에서 내보내기 하세요"만 안내하고 종료 (b) 접근 가능한 모든 원천(저장소 브랜치, 드라이브, Notion, 세션 목록 API, 두 번째 저장소)을 실제로 조회해 옮길 수 있는 것을 전부 옮긴다.
- 선택: (b).
- 이유: CLAUDE.md 1조(실행으로 확인한 뒤에만 "안 된다"고 답한다). 실제 조회 결과 브랜치 14개·드라이브 문서 30개·세션 32개·저장소 1개가 더 나왔다. (a)였다면 7~9월 작업의 대부분을 놓쳤다.
- 버린 이유: (a)는 추론만으로 "불가"라고 답하는 2026-07-03 사례의 반복이다.

### 판단 2 — 7조(첫 작업 요청 시 먼저 질문)와 자율 실행 지시의 충돌
- 분기점: 시스템은 "사용자가 자리에 없으니 질문으로 막지 말라", CLAUDE.md 7조는 "착수 전에 예상 문제를 먼저 질문하라".
- 선택지: (a) 질문만 남기고 대기 (b) 답에 의존하지 않는 작업을 전부 끝내고, 가정과 질문을 최종 보고에 명시.
- 선택: (b).
- 이유: 사용자는 iOS에서 세션을 열고 자리를 비웠을 가능성이 높고, 이 작업의 대부분(조회·요약·스크립트·문서)은 사용자 답과 무관하다. 7조의 목적(예상 문제를 먼저 짚기)은 보고 상단에 "직접 해야 하는 것 4가지"를 두는 것으로 충족된다.
- 버린 이유: (a)는 아무것도 전달하지 못한 채 세션이 끝날 위험이 있다. 이 충돌 해소 규칙을 AGENTS.md 7조에 추가로 적었다.

### 판단 3 — 요약 작성을 누가 할 것인가
- 분기점: 브랜치 1만 9천 줄, WORKLOG 84KB, agents-management 637KB를 읽어야 함.
- 선택지: (a) 상위 모델이 직접 전부 읽는다 (b) Sonnet 하위 에이전트 3개에 병렬로 원본 읽기·요약을 맡기고 상위 모델은 종합 문서(00)·가이드·스크립트·AGENTS.md 변환을 쓴다.
- 선택: (b).
- 이유: 5조. 원본 처리량이 크고 세 원천이 독립적이라 병렬화 이득이 크다. 상위 모델은 요약의 머리 부분과 핵심 절만 읽고 검토했다.
- 버린 이유: (a)는 상위 등급 토큰을 대량 소모하고 세션 컨텍스트를 무겁게 만든다.

### 판단 4 — agents-management의 Claude 호출부를 지금 교체할 것인가
- 분기점: 해지하면 매일 04:00 워크플로가 실패한다는 것을 알게 됨.
- 선택지: (a) run-agent.mjs를 Codex 호출로 고쳐서 푸시 (b) 교체 지점을 줄 번호까지 문서화하고, 별도 작업 카드로 제안하며, 임시로 워크플로 비활성화를 안내.
- 선택: (b).
- 이유: 이 컨테이너에 Codex CLI도 OpenAI 키도 없어 실행 검증이 불가능하다. 2조(검증 후 완료 선언)와 3조(배포되는 자동화는 비가역에 가까움). 사용자 요청 범위도 "컨텍스트 이전"이지 "런타임 재작성"이 아니다.
- 버린 이유: (a)는 검증 안 된 코드를 매일 자동 실행되는 저장소에 넣는 것이다.

### 판단 5 — 산출물을 어디에 둘 것인가
- 분기점: 해지 후에도 사용자가 접근할 수 있어야 한다.
- 선택지: (a) 세션 답변에만 출력 (b) GitHub 브랜치 커밋만 (c) GitHub + 구글드라이브 사본.
- 선택: (c).
- 이유: 3조(파일로 만든다). 세션 답변은 해지 후 접근이 불확실하고, 드라이브는 사용자가 이미 ASTRO SPRINT 원본을 두는 곳이라 발견성이 가장 높다.
- 버린 이유: (a)는 사라진다. (b)는 브랜치가 main에 미병합인 채 잊힐 위험(이미 14개가 그렇다).

### 판단 6 — 9조 판단 기록을 어디에 쓸 것인가
- 분기점: 지정 폴더(G:\)에 접근 불가.
- 선택지: (a) 기록 생략 (b) 팩 폴더에 같은 형식으로 남기고 옮기라고 안내.
- 선택: (b). 이 파일.
- 버린 이유: (a)는 "시점 원칙"(그 세션이 그 시점에 쓴다)을 어긴다.

## 일반화 규칙 (Opus 이식용)
- "이 환경에 없다"는 답은 접근 가능한 모든 원천(저장소 원격 브랜치, 연결된 드라이브·노션, 세션 목록 API, 추가 가능한 저장소)을 실제로 조회한 뒤에만 한다.
- 검증 수단이 없는 코드는 자동 실행 경로에 밀어 넣지 않는다. 교체 지점을 줄 번호까지 문서화하고 별도 작업으로 넘긴다.
- 해지·이전처럼 접근이 끊기는 작업의 산출물은 사용자 소유 저장소 두 곳(코드 저장소 + 문서 드라이브)에 동시에 둔다.
